# Проект: [Название проекта]

## 🚀 Краткое описание

[Краткое описание проекта - что это, для чего предназначено]

## 📦 Быстрый старт

### Требования
- PHP 8.0+
- Веб-сервер (Apache/Nginx)
- База данных (MySQL/PostgreSQL/etc) - если используется

### Установка
```bash
# Клонирование репозитория
git clone [url-репозитория]
cd [название-проекта]

# Настройка веб-сервера
# Настройте виртуальный хост, указывающий на папку public/
```

### Настройка веб-сервера
**Apache (.htaccess уже включен):**
```
DocumentRoot /path/to/project/public
<Directory /path/to/project/public>
    AllowOverride All
    Require all granted
</Directory>
```

**Nginx:**
```nginx
server {
    listen 80;
    server_name your-domain.local;
    root /path/to/project/public;
    
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    
    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }
}
```

## 🐛 Отладка

Проект настроен для отладки с Xdebug в VS Code.

### Быстрый запуск отладки:
1. **Запустите отладчик в VS Code:** F5
2. **Добавьте к URL в браузере:** `?XDEBUG_SESSION_START=VSCODE`
3. **Установите точки останова** в нужных местах кода

### Подробное руководство:
📖 [Полное руководство по отладке](DEBUGGING.md)

**Альтернативные способы:**
- Используйте расширение Xdebug Helper для браузера (но есть подозрение, что он перестал работать)
- Установите cookie: `XDEBUG_SESSION=VSCODE`
- Для CLI: `XDEBUG_SESSION=VSCODE php script.php`

## 🛠 Технологии

- **Backend:** Чистый PHP 8.x, самописный MVC фреймворк
- **Frontend:** Vanilla JavaScript (чистый JavaScript)
- **База данных:** [MySQL/MariaDB]
- **Сервер:** Apache/Nginx с mod_rewrite
- **Отладка:** Xdebug 3.x, VS Code

## 📁 Структура проекта

```
project/
├── .vscode/                 # Конфигурация VS Code
│   ├── launch.json         # Настройки отладки
│   └── settings.json       # Рабочие настройки
├── app/                    # Ядро приложения
│   ├── controllers/        # Контроллеры
│   ├── models/            # Модели
│   ├── views/             # Представления
│   └── core/              # Ядро框架
├── public/                # Public assets
│   ├── js/               # JavaScript файлы (Vanilla JS)
│   ├── css/              # Стили
│   ├── images/           # Изображения
│   └── index.php         # Точка входа
├── config/               # Конфигурационные файлы
├── storage/             # Файлы кэша, логи и т.д.
├── DEBUGGING.md        📖 Подробное руководство по отладке
└── README.md           📖 Этот файл
```

## ⚙️ Конфигурация

### Настройка config файлов
Отредактируйте файлы в папке `config/` согласно вашим настройкам:

**config/database.php** (пример):
```php
<?php
return [
    'host' => 'localhost',
    'dbname' => 'your_database',
    'username' => 'your_username',
    'password' => 'your_password',
    'charset' => 'utf8mb4'
];
```

**config/app.php** (пример):
```php
<?php
return [
    'env' => 'development',
    'debug' => true,
    'url' => 'http://localhost:8000'
];
```

### Настройка VS Code
Проект уже включает предварительно настроенные файлы:
- `.vscode/launch.json` - конфигурация отладчика Xdebug
- `.vscode/settings.json` - рекомендуемые настройки редактора

## 🧪 Тестирование

Так как проект не использует Composer, тестирование можно организовать через простые PHP скрипты:

```bash
# Запуск тестов (если реализовано)
php tests/run.php

# Запуск с отладкой
XDEBUG_SESSION=VSCODE php tests/run.php
```

## 📊 База данных

Если используется база данных:

```bash
# Импорт дампа (пример)
mysql -u username -p database_name < dump.sql

# Или через PHP скрипт миграций
php migrations/run.php
```

## 🚀 Production deployment

```bash
# Переключение в production режим
# Отредактируйте config/app.php:
'env' => 'production',
'debug' => false,

# Убедитесь, что права на папки правильные
chmod -R 755 public/
chmod -R 755 storage/ # если есть логи
```

## 🤝 Разработка

### Code style
Рекомендуется придерживаться PSR-12 стандарта:

```bash
# Ручная проверка стиля кода
# Можно использовать PHP_CodeSniffer вручную
```

### Git workflow
1. Создайте feature branch: `git checkout -b feature/name`
2. Сделайте изменения
3. Протестируйте с отладкой при необходимости
4. Создайте pull request

## ❗ Частые проблемы

### Проблемы с отладкой?
1. Проверьте, что Xdebug установлен: `php -v | grep Xdebug`
2. Убедитесь, что порт 9003 свободен
3. Смотрите подробное руководство: [DEBUGGING.md](DEBUGGING.md)

### Проблемы с веб-сервером?
- Убедитесь, что mod_rewrite включен (для Apache)
- Проверьте настройки виртуального хоста
- Убедитесь, что точка входа - `public/index.php`

### Проблемы с маршрутизацией?
- Проверьте настройки `.htaccess` в папке `public/`
- Убедитесь, что сервер разрешает использование `.htaccess`

## 📞 Поддержка

Если у вас возникли проблемы:

1. **По отладке:** Обратитесь к [DEBUGGING.md](DEBUGGING.md)
2. **По установке:** Проверьте настройки веб-сервера и PHP
3. **По маршрутизации:** Проверьте `.htaccess` и настройки сервера

**Логи для диагностики:**
- Логи Xdebug: `/tmp/xdebug.log`
- Логи веб-сервера: 
  - Apache: `/var/log/apache2/error.log`
  - Nginx: `/var/log/nginx/error.log`
- Логи приложения: `storage/logs/` (если реализовано)

## 📝 Лицензия

Этот проект лицензирован под [название лицензии] - см. файл [LICENSE](LICENSE) для деталей.

---

**Примечание:** Для эффективной разработки рекомендуется использовать VS Code с расширением PHP Debug и ознакомиться с [руководством по отладке](DEBUGGING.md). Проект использует чистый PHP без внешних зависимостей.